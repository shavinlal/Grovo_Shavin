  test:
    override:
      - pybot -n non-critical -d "robot_results" -o "../discard/output.xml" ./AutomationTestcase.postman_collection.JSON.parse
  general:
    artifacts:
      - "robot_results" 
