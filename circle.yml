job:
  test:
    working_directory: ~/Grovo_Shavin
    override:
      - pip install -r requirements.txt
      - pybot -n non-critical -d "robot_results" -o "../discard/output.xml" ./TestSuite1.robot
workflows:
  version: 2
    triggers:
      - schedule:
          cron: "16 5 * * *"
          filters:
            branches:
              only:
                - master
                - beta
    jobs:
      - test



   
